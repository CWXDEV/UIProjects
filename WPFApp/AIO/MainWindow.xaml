<mah:MetroWindow x:Class="wpfAppMetro.MainWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                 xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
                 xmlns:core="clr-namespace:wpfAppMetro.Core"
                 xmlns:viewModels="clr-namespace:wpfAppMetro.ViewModels"
                 mc:Ignorable="d"
                 Height="700"
                 Width="1100"
                 WindowStartupLocation="CenterScreen"
                 ResizeMode="CanMinimize">

	<!-- https://github.com/MahApps/MahApps.Metro -->

	<Window.DataContext>
		<viewModels:MainViewModel />
	</Window.DataContext>

	<mah:MetroWindow.LeftWindowCommands>
		<mah:WindowCommands>
			<Button Click="LaunchGitHubSite" ToolTip="Open Repo for this app">
				<iconPacks:PackIconModern Width="28" Height="28" Kind="SocialGithubOctocat" />
			</Button>
		</mah:WindowCommands>
	</mah:MetroWindow.LeftWindowCommands>

	<mah:MetroWindow.RightWindowCommands>
		<mah:WindowCommands>
			<Button ToolTip="Settings" Command="{Binding SettingsViewCommand}">
				<iconPacks:PackIconModern Width="18" Height="18" Kind="Cog" />
			</Button>
		</mah:WindowCommands>
	</mah:MetroWindow.RightWindowCommands>

	<Grid>
		<StackPanel x:Name="SideMenu" Width="45" HorizontalAlignment="Left" Background="#292929" ZIndex="2"
		            MouseEnter="SideMenu_OnMouseEnter" MouseLeave="SideMenu_OnMouseLeave">

			<!-- Add new icon Radio Button to get a new menu item -->
			<core:IconRadioButton Content="Home" Icon="Home" IsChecked="True" Command="{Binding HomeViewCommand}" />
			<core:IconRadioButton Content="Hardware Monitor" Icon="Thermometer" Command="{Binding HardwareMonitorViewCommand}" />
			<core:IconRadioButton Content="Spt Launcher" Icon="Rocket" Command="{Binding SptLauncherViewCommand}" />

			<StackPanel.Resources>

			</StackPanel.Resources>
		</StackPanel>

		<ContentControl Margin="65, 20, 20, 20" Content="{Binding CurrentView}" />
	</Grid>

</mah:MetroWindow>