<UserControl x:Class="wpfAppMetro.Views.HardwareMonitorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:wpfAppMetro.Views"
             xmlns:viewModels="clr-namespace:wpfAppMetro.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="300" 
             d:DesignWidth="600"
             Background="Transparent">
    
    <UserControl.DataContext>
        <viewModels:HardwareMonitorViewModel />
    </UserControl.DataContext>
    
    <StackPanel x:Name="CpuContainer">
        <!-- <TextBlock Text="{Binding CPUload.Value}" Foreground="White" FontSize="20" VerticalAlignment="Top" HorizontalAlignment="Center"/> -->
        <ItemsControl ItemsSource="{Binding LoadSensors}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding }" Foreground="White" FontSize="20" VerticalAlignment="Top" HorizontalAlignment="Center"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>
