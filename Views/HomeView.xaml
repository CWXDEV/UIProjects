<UserControl x:Class="AIO.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:AIO.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="300"
             Background="Transparent">

	<UserControl.DataContext >
		<viewModels:HomeViewModel />
	</UserControl.DataContext>

	<Grid>
		<TextBlock Text="HOME PAGE" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{StaticResource AioWhite}" FontSize="33" />
	</Grid>
</UserControl>